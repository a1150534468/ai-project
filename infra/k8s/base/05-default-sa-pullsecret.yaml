# Optional image pull secret placeholder. Replace the name in your private
# deployment overlay; no real registry credential is stored in this repo.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: default
  namespace: ai-assistant
imagePullSecrets:
  - name: image-pull-secret-placeholder
