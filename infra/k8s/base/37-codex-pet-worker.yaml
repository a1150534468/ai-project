apiVersion: apps/v1
kind: Deployment
metadata:
  name: codex-pet-worker
  namespace: ai-assistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codex-pet-worker
  template:
    metadata:
      labels:
        app: codex-pet-worker
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8092"
        prometheus.io/path: "/metrics"
    spec:
      terminationGracePeriodSeconds: 180
      imagePullSecrets:
        - name: image-pull-secret-placeholder
      containers:
        - name: codex-pet-worker
          image: ai-assistant-api
          command:
            - node
            - --import
            - tsx
            - apps/api/src/workers/codex-pet-worker.ts
          envFrom:
            - configMapRef:
                name: ai-assistant-api-config
            - secretRef:
                name: ai-assistant-api-secrets
          ports:
            - name: health
              containerPort: 8092
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: health
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet:
              path: /health
              port: health
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: health
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 4
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
              ephemeral-storage: "4Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
              ephemeral-storage: "8Gi"
