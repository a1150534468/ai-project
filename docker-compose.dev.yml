services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: yunclaude
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres-password-placeholder}
      POSTGRES_DB: yunclaude
    ports: ["5433:5432"]
    volumes: ["yc_pg:/var/lib/postgresql/data"]
  redis:
    image: redis:7
    ports: ["6380:6379"]
  billing-postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: ycbilling
      POSTGRES_PASSWORD: ${BILLING_POSTGRES_PASSWORD:-billing-postgres-password-placeholder}
      POSTGRES_DB: ycbilling
    ports: ["5434:5432"]
    volumes: ["yc_billing_pg:/var/lib/postgresql/data"]
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minio-access-key-placeholder}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minio-secret-key-placeholder}
    ports: ["9000:9000", "9001:9001"]
    volumes: ["minio-data:/data"]
volumes:
  yc_pg:
  yc_billing_pg:
  minio-data:
