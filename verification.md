# 资源优化与生产部署验证记录

验证日期：2026-08-21

## 结论

- Billing 保持完整启用，接口、Schema、页面和对账任务未增加关闭开关。
- API 与统一 Worker 均在生产镜像内使用 `tsx` 直接运行 TypeScript 源码，未启用 watch。
- 三个业务 Worker 已合并为一个 Node 进程，默认队列并发和进程内重任务并发均为 `1`。
- 生产 Compose 不包含 Kubernetes、MinIO、Vite 或额外前端 Node 进程；Web/Admin 由 Gateway 提供静态文件。
- 本地发布门槛与隔离生产拓扑均已通过，可以进入 Commit、推送和目标服务器部署阶段。
- 本地资源数据只能证明拓扑与优化方向有效，不能替代目标 `4C4G` 服务器验收。

## 发布门槛

| 项目 | 结果 |
| --- | --- |
| 全仓类型检查 | 11/11 包通过 |
| 全仓测试 | 10/10 测试任务通过；API 1815 通过、18 跳过 |
| 全仓构建 | 通过；Web 有既有约 1.72 MB 大 chunk 警告 |
| Billing Go 测试 | 全部通过 |
| 改动文件 `noUnusedImports` 检查 | 63 个文件通过 |
| Workflow YAML、Compose、Shell 语法 | 通过 |
| 敏感信息扫描 | 待提交改动未发现私钥、COS SecretId 或常见 API Key |
| API、Gateway、Migration、Billing 镜像构建 | 全部通过 |
| 主库全新环境迁移 | 73 个 Prisma 迁移全部成功 |
| Billing 全新环境迁移 | 通过 |
| 隔离生产拓扑冷启动 | 7 个常驻服务全部健康 |
| App/Admin/API/Billing Host 路由 | 全部通过 |

完整测试最初因本机 Billing 占用 `8093` 而使 13 个“Billing 不可达应返回 502”的用例失败。生产工作流已调整为先运行 Node 测试与构建，再启动真实 Billing 做独立健康冒烟；测试 URL 仍保留 OpenAPI 契约要求的 `8093`。复测后 Billing 相关失败已清零。

全仓并行测试还发现 `RippleButton` 卸载后遗留定时器。组件现会在卸载时清理自身 timeout，并新增回归用例；修复后全仓测试连续完整结束，没有测试环境销毁后的异步 React 错误。

首次生产工作流的 Node 验证全部通过，但 Billing 的会员周期测试曾隐式使用 Runner 本地时区，因 GitHub 使用 UTC 而失败。生产实现始终固定使用北京时区，本次仅将测试输入和预期显式锚定 `BeijingLoc`；Billing 全量 Go 测试已在 `TZ=UTC` 下复测通过。

第二次生产工作流的验证、Gateway 和 Billing 镜像均通过，但 API 与 Migration 镜像在 GitHub 海外 Runner 上持续使用国内 apt/npm/Prisma 镜像而未完成。构建源现已参数化：本地默认继续使用国内镜像，CI 显式使用官方源，并为 apt、npm、Prisma 下载增加重试和镜像 Job 超时。

修正后的 API 已使用 CI 官方源参数完整构建成功，Migration 分别完成官方源构建和默认国内源最终 Dockerfile 构建，两个 Prisma 引擎均可运行。新镜像再次通过全新数据卷迁移、7 服务健康和四个 Host 路由的隔离生产拓扑冒烟。

最新镜像大小（Docker 报告的十进制字节换算）：

| 镜像 | 大小 |
| --- | ---: |
| API/Worker | 约 497 MB |
| Gateway | 约 22 MB |
| Billing | 约 5.6 MB |
| Migration（仅迁移时运行） | 约 249 MB |

## 本地资源数据

历史同一轮资源优化采样结果：

| 场景 | 结果 |
| --- | ---: |
| API 延迟加载 sharp 前 | 约 330 MiB |
| API 延迟加载 sharp 后的稳定样本 | 约 184 MiB |
| 三个独立 Worker | 约 373 MiB |
| 合并 Worker | 约 166 MiB |
| Worker 合并收益 | 约 207 MiB |
| 延迟加载后的完整空闲拓扑（含 Gateway） | 约 472 MiB |

最新镜像使用全新空数据卷完成迁移和冷启动后的瞬时样本：

| 服务 | RSS |
| --- | ---: |
| API | 约 265 MiB |
| 合并 Worker | 约 185 MiB |
| Gateway | 约 49 MiB |
| 主 PostgreSQL | 约 69 MiB |
| Billing PostgreSQL | 约 52 MiB |
| Redis | 约 13 MiB |
| Billing | 约 10 MiB |
| 合计 | 约 644 MiB |

该数据处于冷启动阶段，只用于确认当前镜像没有异常增长或 OOM，不作为 30 分钟空闲基线。先前 Redis 故障注入中，API 在 `2064 ms`、Worker 在 `2033 ms` 返回 `503`；Redis 恢复后两者重新返回 `200`，没有容器重启或 OOM。

## 目标服务器待验收

服务器、域名、COS、TCR、GitHub Actions Secrets 和生产环境文件已准备完成。剩余步骤：

1. 创建本地 Commit，经用户确认后推送 `main`，触发镜像构建和自动部署。
2. 容器全部健康后安装宿主机 Caddy 站点片段，不停止或替换现有 DSH Caddy。
3. 验证四个生产域名、登录、对话、知识库、完整 Billing 和 COS 上传下载。
4. 保持服务器上的另一应用运行，执行 30 分钟空闲和最多五用户普通场景。
5. 小说、商家宣传视频、Codex Pet 三类重任务逐类至少执行三次，检查峰值和 RSS 回落。
6. 完成不少于 2 小时稳定性采样，检查 CPU、Swap、OOM、队列积压和接口延迟。
7. 逐个重启服务并执行一次上一 Commit SHA 回滚，再发布当前版本。

目标区间不是 cgroup 硬限制：空闲约 `1.1-1.4 GiB`，普通五用户约 `1.4-1.8 GiB`，单个重任务尽量接近 `2 GiB`。不得出现 OOM、持续增长的 Swap、任务完成后内存不回落或另一应用明显变慢。

## 清理状态

隔离验证使用的 `ai-assistant-final-verify` 容器、网络和数据卷已全部删除。本地开发 PostgreSQL、Redis、MinIO、Billing 以及其他应用未停止或修改。四个 `:verify` 镜像标签保留在本机，未推送远程仓库。

## 万物试穿功能验证（2026-08-31）

- API 试穿提示词、路由与后台菜单测试：18/18 通过。
- Web 工作台、页内菜单、API 客户端测试：19/19 通过。
- API 与 Web TypeScript 类型检查通过；Web 生产构建通过。
- 11 个改动代码文件 Biome lint 与 `git diff --check` 通过。
- 在现有 `http://localhost:5174` 登录态下完成桌面和 390 x 844 窄屏实测：页内标签、三个通用素材槽、说明、生成按钮、预览及历史区域正常，试穿工作台内无文字遮挡。
- 生产构建仍有既有约 1.73 MB 大 chunk 警告，本次改动未新增依赖或扩大打包边界。

## 商品提取功能验证（2026-08-31）

- Web 商品提取提示词、上传提交、Hub 行为与菜单测试：55/55 通过。
- API 后台菜单目录与路由测试：7/7 通过。
- Web TypeScript 类型检查、变更文件 Biome lint、`git diff --check` 与生产构建通过。
- 在现有 `http://localhost:5174` 登录态下完成桌面和 390 x 844 窄屏检查：单原图、商品描述、模型、比例、分辨率、费用、结果区和历史区布局正常。
- 窄屏下五个生图标签保持单行并横向滚动；商品工作台宽度 `343/343`，没有横向溢出或控件重叠。
- 未上传真实商品、未调用真实生图上游、未产生扣点；白底平铺效果由固定提示词契约和请求载荷测试覆盖。
- 生产构建仍有既有约 1.73 MB 大 chunk 警告，本次未新增依赖或部署变量。

## 完整生图模块误删恢复（2026-09-18）

### 恢复范围与原因

- 误删来自 `1a4a0f2`（入口/前后端）、`92bdb30`（素材来源）、`9b72472`（数据模型与删表迁移）。用户要求保留的是完整生图模块，不是仅通用生图；已纠正解耦方案、ADR-012 和生图文档，防止继续按错误范围删除。
- 恢复五个场景：通用生图、电商图（商品主图、详情长图、分段重绘、浏览器拼接、文案辅助）、商品提取、形象照（16 个模板）、万物试穿（包含模特试穿）。恢复相应菜单、接口、任务/历史、素材库来源和八张数据表。
- 沿用当前工作台外壳与共享生图服务；首次访问挂载工作台，页内切换保留已填写草稿；通用生图与商品提取历史隔离。
- 不复活已移除的计费服务。旧任务计费字段只为兼容保留，默认实现不发起计费网络请求；这不表示上游模型调用免费。

### 验证结果

| 验证 | 结果 |
| --- | --- |
| Web 全量 | 92 文件、895 测试通过 |
| API 全量（独立 PostgreSQL 数据库 + 独立 Redis） | 134 文件通过、6 文件跳过；1293 测试通过、18 跳过，无失败 |
| 管理端 | 10 文件、109 测试通过 |
| 共享包 | llm 49 通过/5 跳过；article-workflow 27、novel-workflow 13、codex-pet-pipeline 54 通过 |
| `pnpm typecheck` | 8/8 任务通过 |
| `pnpm build` | 2/2 任务通过；仍有 Web 大 chunk 提示，非构建失败 |
| 本次 82 个改动代码文件 Biome lint | 通过 |
| 历史迁移完整性 | 82 个历史文件校验通过；未修改旧迁移 |
| 数据库迁移 | 本地已有数据库新增恢复迁移成功；独立空库完整 83 个迁移成功 |
| 真库生命周期新增回归 | 形象照/试穿上传、生成、下载、幂等、用户隔离、历史、删除均通过；生成器和对象存储注入测试替身，默认零值兼容实现未替换 |
| 本地真实 HTTP/MinIO | 健康、两场景 options/state、电商主图/详情 current/history/pricing、素材来源筛选共 14 个 GET 返回 200；两场景上传、签名图片读取、错误签名拒绝、未登录 401、删除后不可读取通过 |
| 浏览器实测 | 五场景可切换；形象照/试穿草稿保留；主图/详情共用商品资料；商品提取缺图提示；素材库形象照/试穿筛选；桌面与 390×844 试穿布局正常；所检查页面无控制台 error |
| 最后健康检查 | `http://localhost:5174`、`http://127.0.0.1:8090/health` 均为 200 |

API 跳过项为 16 项显式 opt-in 的模型/embedding POC、1 项退役桌宠夹具、1 项通用 S3 往返（隔离套件故意未配置 S3；已另外完成上面的真实 MinIO 场景验证）。共享 llm 的 5 项为未启用的上游集成验证。未调用真实收费模型，不能把测试替身出图等同于真实模型效果验收。

早期回归失败已定位并处理：素材库新增来源后同时间排序断言补全；隔离环境移除不应配置的假 S3 地址；新生命周期测试纠正夹具对象键、响应字段和无签名请求的状态码。最后全量回归无失败。格式化后新增生命周期 2 项和 Hub 4 项也分别复跑通过。

### 数据、发布与清理边界

- 新迁移：`packages/db/prisma/migrations/20260918100000_restore_image_workflows/migration.sql`，事务内新增八张表、索引和外键；没有删除现有表或改写历史迁移。
- 本地数据库已执行过 `20260904080000_drop_retired_modules`，因此这里只恢复结构，**没有找回被旧迁移清掉的历史记录**。恢复历史数据需要删表前备份；本次没有对生产数据做任何操作。
- 发布前必须先备份并核对目标数据库迁移状态。**如果旧删表迁移仍未执行，不能直接跑整条迁移链**，需先另行制定保数据迁移方案；已执行旧删表迁移的环境，确认备份后再部署新增恢复迁移、生成 Prisma 客户端、构建并重启 API/Web。没有新增必填环境变量，沿用现有存储与模型配置。
- 回滚优先回滚应用版本，保留恢复后的数据表，不执行 DROP TABLE，不重跑旧删除迁移。
- 本次未 commit/push、未部署生产。临时 QA 用户及其上传、独立 QA 数据库和独立 Redis 容器已清理；原有本地开发服务保留运行。临时日志放在 `/tmp/restore-image-qa/`，代码仓库未保留过程快照。

## 生图历史数据选择性恢复（2026-09-18）

本节更新上文“只恢复结构、未找回历史”的阶段性结论：已根据用户要求，从本地删表前备份 `/Users/z/backups/aiproject-decouple-2026-09-04/main-before-drop.dump` 中，仅补回生图模块历史数据，未整库回滚。

| 数据表 | 补回记录数 |
| --- | ---: |
| PortraitReferenceAsset | 2 |
| PortraitTask | 22 |
| PortraitOutput | 16 |
| TryOnReferenceAsset | 2 |
| TryOnTask | 1 |
| TryOnOutput | 1 |
| EcomWorkflow | 3 |
| EcomMainImageJob | 2 |
| 合计 | 49 |

- 恢复前已另存这八张表的备份；目标表为空检查、表锁、外键约束和单事务保护均启用。恢复后的行指纹与源备份完全一致；全 public 表前后指纹确认仅上述八张表改变，其他模块未改动。
- 共享 `ImageAsset` 的 89 条、`ImageGenerationTask` 的 13 条现有记录保持不变，已覆盖备份中的相应 ID，无需覆盖较新的数据。
- 16 张形象照与 1 张试穿成品通过实际签名下载接口全部返回 HTTP 200，字节大小匹配且图片可解码；电商任务关联的 11 张现有图片全部可读取、解码，所属用户关系正确。
- 4 条参考图记录在源备份中已被软删除，文件也已不存在；保留原删除状态，未伪装成可用素材。失败任务和原始时间戳同样保留。所恢复任务均为终态，未触发重新生成或付费模型调用。
- 恢复范围以 2026-09-04 的源备份为准，不代表找回该备份之后的全部历史。本次仅修改本地数据，没有生产操作、Schema 变更或远程推送。
- 最终 API `/health` 与 Web `http://localhost:5174` 均返回 HTTP 200。隔离暂存数据库与 402 MB 临时提取 SQL 已清理，原始备份及恢复审计材料保留；原有开发服务保持运行。
- 审计与恢复前备份：`/Users/z/backups/aiproject-image-restore-2026-09-18/`。其中 `restore-summary.json`、前后指纹、`verification.json`、`storage-check.json` 和事务日志记录核验结果；文件包含用户数据，仅保留在仓库外的私有目录。恢复脚本已执行成功，不应再次运行。

## 生图模块生产发布前检查（2026-09-18）

- 已获用户明确授权提交、推送并发布生产；本次发布代码与新增恢复迁移，不上传本地历史数据或恢复 SQL。
- 生产当前镜像为 `462c2cfd3d5993c2515216ab15aca235db05b123`。生产数据库已完成 `20260904080000_drop_retired_modules`，恢复目标表不存在，可执行本次只新增八张表的迁移，无需重跑旧删除迁移。
- 生产主库已用 `pg_dump -Fc` 备份并通过 `pg_restore --list` 检查，保存在服务器 `/opt/ai-assistant/backups/20260918-restore-image-workflows/main-before-release.dump`，同目录保存上一镜像标签；备份仅服务器私有保存。
- 发布前重跑 `pnpm typecheck`（8/8）、`pnpm build`（2/2）和 `git diff --check` 均通过。构建保留已有大 chunk 提示。
- 按现有流程先推送 `main` 并等待 CI，再手动触发生产工作流；部署后核对镜像标签、迁移及服务健康。生产历史记录不从本地搬迁；回滚仅回退应用镜像，保留新建表。
- 首轮远程 CI：API 1293 项通过、18 项预期跳过；其他包通过，Web 894 通过/1 失败。唯一失败是原有 `MemoryDetailPanel.test.tsx` 在退出动画完成前断言输入框已卸载：`findByText(NODE.text)` 也能匹配尚未卸载的 textarea，不能代表查看态就绪。仅将取消/保存成功两处测试改为等待标题输入框消失，再断言正文；未修改记忆模块业务代码、未跳过测试。修正后该文件 15/15、本地 Web 全量 895/895 通过。
