# 2026-09-19 性能优化第一批发布

## 范围和开关

- 小说项目/工作台 compact API，避免目录读取附带全篇正文、上下文和生成快照；旧 full 接口兼容。
- 章节按需读取、同身份 GET 在途去重、刷新合并、草稿与项目切换保护。
- 通用图片、形象照、试穿和桌宠预览/动画图交付支持 proxy/COS/CDN；ZIP 保持原路径。
- **本批保持 proxy，CDN 未启用**。生产 `WORKFLOW_IMAGE_DELIVERY` 未设置即默认 proxy。无生产环境变量、数据库 Schema 或云配置修改。
- 不包含真实付费生成、实际桌宠客户端安装。

## 验证与发布门槛

- 本地全仓回归 2599 passed / 23 skipped（隔离 PostgreSQL/Redis，不连接业务库）；类型检查、构建、变更文件 lint 通过。
- 必须等待本批精确提交的 main CI 通过，再运行既有 production 工作流。
- 部署前检查队列和生成任务，记录旧镜像；上线后检查容器健康、小说 full/compact/单章一致性、鉴权及图片/桌宠代理。
- 2026-09-19 发布前旧镜像：`8ed93c54182471e3fc90202bbd49b43bd67576c9`。实际新版本/运行编号及验收结果记录在本地 `.codex/requirements/20260919-media-delivery/publish/`；本文不是部署完成证明。

## 未完成清单

1. **CDN 安全参数约束和启用**：工单处理中。AccessControl 配置失败；有效签名附加响应覆盖参数能改变返回 MIME，尚未闭环。不代表已证实鉴权绕过或缓存投毒。用户负责云配置修改，不操作另一项目域名。需完成参数限制、鉴权、缓存和浏览器 Blob/下载验收后，再单独切换。
2. **图片出口瓶颈仍在**：当前代理仍共享服务器 3 Mbps 出口；不能承诺图片/桌宠变快或图片加载时所有接口不卡。
3. 缩略图、图片压缩/尺寸分级和进一步懒加载未做。
4. 前端大包拆分、首屏与帧率专项优化未做。
5. 桌宠真实客户端安装未验收。
6. 真实网络下普通接口与图片并发的 P50/P95、首屏、带宽及长期稳定性待测。合成样本约 98.3% 的响应减量不是生产提速倍数。

## 回滚

本批无新 Schema、环境设置不变。若回归，在生产执行既有脚本（仅本项目）：

```sh
DEPLOY_ROOT=/opt/ai-assistant bash /opt/ai-assistant/rollback-compose.sh 8ed93c54182471e3fc90202bbd49b43bd67576c9
```

随后核对五容器健康、旧镜像标签和业务只读冒烟。健康等待失败时部署脚本会尝试回滚，但拉镜像/迁移/up 阶段提前退出并非总能自动回滚，须人工核实。不执行 `down -v`，不删除卷。
